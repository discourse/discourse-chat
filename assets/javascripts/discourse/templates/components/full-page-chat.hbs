<div class={{wrapperClassNames}}>
  {{#if (or (and site.mobileView showingChannels) (and (not teamsSidebarOn) (not site.mobileView)))}}
    {{channel-list
      publicChannels=publicChannels
      directMessageChannels=directMessageChannels
      onSelect=(action "switchChannel")
    }}
  {{/if}}
  {{chat-live-pane
    chatChannel=chatChannel
    expanded=true
    floatHidden=false
    fullPage=true
    onBackClick=(action (mut showingChannels) true)
  }}
</div>
